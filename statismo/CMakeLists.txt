
FIND_PACKAGE(STATISTICAL_MODEL)
SET(CMAKE_VERBOSE_MAKEFILE ON)
include_directories (${STATISTICAL_MODEL_INCLUDE_DIR}) 

FILE(GLOB INCLUDE_FILES *.h)
FILE(GLOB SOURCE_FILES *.cxx *.txx)
add_library (StatisticalModel statismo.cxx

	${INCLUDE_FILES}
	${SOURCE_FILES}
	)
